<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Phone_Required_For_Conversion</fullName>
    <active>true</active>
    <description>Prevents a Lead from being converted without a phone number. Ensures data quality and completeness for converted leads.</description>
    <errorConditionFormula>AND(
   ISPICKVAL(Status__c, &quot;Converted&quot;),
   ISBLANK(Phone__c)
)</errorConditionFormula>
    <errorDisplayField>Phone__c</errorDisplayField>
    <errorMessage>Phone Number is required before converting a Lead. Please enter a valid phone number.</errorMessage>
</ValidationRule>